# Server Component

- Everything is rendered in server first even in `"use client"`.
- In normal render (server rendering), you can put API, fetch data because it is only rendered in the server.

![NextJS_Server](img/nextjs_server.png)
