# Layouts

- Layouts are nested

```
<Layout>
    <SecondLayout>
        <Component />
    </SecondLayout>
</Layout>


```

- `Layout` is the main layout in app folder.
- `SecondLayout` is the layout that inside another folder in app folder.
